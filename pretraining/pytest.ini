[tool:pytest]
# Tell pytest where to find tests
testpaths = tests

# Test file patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Show more detail in output
addopts = -v

# Markers for categorizing tests (optional)
markers =
    slow: marks tests as slow
    unit: unit tests
    integration: integration tests
